<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <!-- Set AppCompat’s color theming attrs -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="android:activatedBackgroundIndicator">@drawable/activated_background</item>
        <item name="searchViewStyle">@style/MSearchViewStyle</item>
        <!--<item name="colorControlNormal">@color/white</item>-->
        <item name="windowActionModeOverlay">true</item>

        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

    </style>

    <!-- https://philio.me/styling-the-searchview-with-appcompat-v21/ -->

    <style name="MSearchViewStyle" parent="Widget.AppCompat.Light.SearchView">
        <item name="searchIcon">@drawable/ic_action_search_light</item>
        <item name="closeIcon">@drawable/ic_action_cancel</item>
        <item name="queryBackground">@android:color/white</item>
        <item name="submitBackground">@android:color/white</item>
        <item name="colorControlNormal">@color/toolbargrey</item>
        <!--<item name="suggestionRowLayout">@layout/abc_search_dropdown_item_icons_2line</item>-->
        <!--<item name="suggestionRowLayout">@layout/suggestion_row_layout</item>-->
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/background_material_light</item>
    </style>

    <style name="AppTheme.Translucent" parent="AppTheme">
        <item name="android:windowNoTitle">true</item>
        <!--item name="android:windowBackground">@android:color/transparent</item-->
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>


    <style name="Widget.SIIGMobile.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="theme">@style/ThemeOverlay.SIIGMobile.ActionBar</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <style name="ThemeOverlay.SIIGMobile.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
    </style>


</resources>
